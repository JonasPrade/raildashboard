# Schienendashboard – test database for db_related_tests
#
# Copy this file to apps/backend/env.test (no leading dot!).
# Loaded directly by tests/db_related_tests/conftest.py via python-dotenv.
#
# The DATABASE_URL MUST contain the word "test" — the test suite enforces this
# to prevent accidental data loss on a production database.
#
# Use the same DATABASE_URL as in /.env.test (repo root).
# Get the connection string from the project notes (Zettel).

DATABASE_URL=postgresql+psycopg2://user:password@host:port/test_schienendashboard
