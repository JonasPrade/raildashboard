#!/bin/bash
# Scoped pytest runner — only runs pytest from the backend venv.
# Prevents arbitrary python execution via python:* wildcards.
set -e
cd /home/phips/code/raildashboard/apps/backend
exec .venv/bin/pytest "$@"
